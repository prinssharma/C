#include<stdio.h>
int main()
{
    float a,b,c;
    printf("Enter three no. ");
    scanf("%f %f %f",&a,&b,&c);
    if(a>b);
    {
        if(a>c);
        printf("a is big");
    }
    else
    {
        printf("c is big");
    }
    else
}
